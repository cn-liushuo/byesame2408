---
description: 代码、注释、逻辑与 TypeScript 习惯
alwaysApply: true
---

# 代码、注释与逻辑

- 生成代码前先确认技术栈（语言版本、框架、现有目录与写法）。
- **注释**：与当前文件风格一致；**业务含义、接口约定、非显而易见的算法或状态**须用**中文**说明「为什么」与边界；显而易见的一行不写重复注释。关键处不可无注释，避免堆砌废话。
- 复杂逻辑需必要错误处理或说明，避免静默失败。
- 禁止假设不存在的外部依赖；若需新库须说明理由。
- 需求模糊或缺少上下文时**先提问**，禁止盲猜业务或接口字段。
- API 路径、参数与事实描述须有据可查，禁止编造接口。

## 函数与 TypeScript 习惯

- 命名：`camelCase`，动词开头（如 `get`、`fetch`、`handle`、`is`、`format`），避免无意义缩写。
- 参数：不宜过多；超过约 3 个时改为「对象入参 + 解构」。合理默认值；**不要直接改传入的对象/数组引用**，需变更时返回新对象。
- 结构：单一职责、优先提前返回（early return）控制嵌套；单函数体量建议可控（如不超过约 50 行，视项目惯例微调）。
- 异步：统一 `async/await`，错误用 `try/catch`；循环内避免无脑串行 `await`，可并发处用 `Promise.all` 等。
- 类型：函数参数与返回值尽量显式类型；避免滥用 `any`，优先接口与泛型。
- **对外导出**的工具函数、公共模块：可写 **JSDoc**（用途、`@param`、`@returns`）；页面与组件内联逻辑以**中文注释**为主，与仓库现有风格一致。
